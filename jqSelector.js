$(document).ready(()=>{
    $("#myId"); //id selector in jQuery
    $(".myClass"); //class selector in jQuery
    $("div"); //element selector in jQuery
    $("input[type='first_name']"); //attribute selector in jQuery
    $("#contents ul.people li"); //descendant selector in jQuery
    
})